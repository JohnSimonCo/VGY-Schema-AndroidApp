<resources>

    <style name="BaseTheme" parent="@android:style/Theme.Holo.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
    </style>

    <style name="AppTheme" parent="@style/BaseTheme">
        <item name="android:windowBackground">@color/bg</item>
    </style>

</resources>
